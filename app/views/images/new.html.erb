
<h1>New Upload</h1>
 
<%= form_for(@dante) do |f| %>
  <div>
    <%= f.label :image %><br>
    <%= f.file_field :image %>
    <%= f.hidden_field :user_id, { :value => session[:user_id] } %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
 
<%= link_to 'Back', images_path %>
